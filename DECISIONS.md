# 决策日志

判据只有一条：**任何一个口径，如果只能靠读 issue 链考古才查得到，它就该在这个文件里。**

一行一条：生效日 / 决定 / 出处 / 取代了哪条 / 现在去哪儿验证它还成立。
新决定追加在表末；推翻旧决定时不要删行，另起一行并在「取代」列写明取代了谁。

来源：#134（治理工作包）。相关：#102（简洁守恒五则）、#85。

## 已生效

| 生效日 | 决定 | 出处 | 取代 | 现在在哪儿验证 |
| --- | --- | --- | --- | --- |
| 2026-07-11 | 每日完成态以「完整做完一组」为准；提前收口只在基础短组确实全部覆盖时才记今日完成，未完成组不点亮完成态 | CHANGELOG 2026-07-11 / 2026-07-16 | — | `index.html` 的 `completedRoundIds` / `completedGroups`；`activity.daily` 每日行 |
| 2026-07-12 | streak 口径改为**累计练习日**：非连续、无断签清零；当天完整做完第一组 +1 天，同日加练不重复计；迁移日之前的历史打开天数全额继承 | CHANGELOG 2026-07-12 | 取代「连续天数」口径，连续口径的展示与迁移桥接同日下线 | `totalPracticeDays()`；首页 chip「拾字第 N 天」 |
| 2026-07-19 | 红色经济两法：① 一屏至多一个实心朱红，它就是本屏主行动，其余状态只用墨阶、砂色和结构表达；② 印章语义只许两种——品牌，以及完成后留下的刻痕 | CHANGELOG 2026-07-19 | 黑色按钮作为行动样式的用法同日废止 | `scripts/verify_redesign_v3.js:34` 常驻断言（改坏了会红） |
| 2026-07-19 | 汉字数字：标题级大字里的数字一律用汉字数字；正文、统计和日期保留阿拉伯数字 | CHANGELOG 2026-07-19 | — | `scripts/verify_ui_copy.py`；CHANGELOG 2026-07-19 条目 |
| 2026-07-28 | 字库以「通用规范汉字表」为唯一权威来源，库的口径与总数随表走，不随产品心情走 | CHANGELOG 2026-07-28 | — | `scripts/verify_library_governance.py` |
| 2026-08-07 | **四库方案**：规范常用字 3500 / 规范次常用字 3000 / 规范专门用字 1605 / 义教基础字 2500；缺笔顺数据的字不进练习，所以实际可练数小于官方总数 | #119 | 取代 #106 的六库方案（六库上线 9 天后被推翻，#106 已 CLOSED） | `index.html` 的 `LIBRARIES`；`verify_pwa_upgrade.js` 的 `EXPECTED_LIBRARIES` 钉死 3500/2976/818/2500 |
| 一直如此 | 提醒默认关：`enabled` 只认显式 `true`，任何缺失或脏值都归为关 | #46 执行口径「同期不做提醒臂、不改 streak 压力」 | — | `index.html` `normalizeReminder()` |
| 一直如此 | 盖章停留 **1800ms**（校准首卡同值），撤销窗口同为 1800ms | — | #134 正文里记的「1100ms」是过期数字，以代码为准 | `index.html` `STAMP_HOLD_MS` / `CALIBRATION_FIRST_HOLD_MS` / `EDIT_STAMP_WINDOW_MS` |
| 一直如此 | 全 App 只有**一个**「独立写出」口径：`outcome === "fast"`。它同时对应 FSRS 的 `Good`、`reason:"independent"` 与 `activity.independentTargetKeys`。`slow` 写出来了但不算独立，`hinted` 用了提示，`miss` 没写出来 | — | — | `index.html` `recordOutcome()` / `markPracticeStamp()`；`scripts/verify_backup_summary.py` 用它算疗效 |
| 一直如此 | 本地优先：`localStorage` 是唯一存储，零主动收集，无服务端、无信标；拍字照片只在本机识别 | README | — | `BACKUP_KEYS`；全仓无上报端点 |
| 2026-08-30 | 汇总脚本的留存分母用**备份导出日**当观察终点，不用「最后一次打开」——后者会把打开一次就流失的人整个删失掉。单日与区间两种口径并列输出 | #130 | 取代原 `observed >= d1` 的分母 | `scripts/verify_backup_summary.py` 的合成队列断言（旧分母 100% vs 新分母 66.7%） |

## 待裁定

这些是规矩已经写好、但当前实现与它不一致的地方。留在这里而不是默默放过。

| 项 | 现状 | 冲突的规矩 | 需要的裁定 |
| --- | --- | --- | --- |
| 「好字成卡提示」默认状态 | **默认开**（`normalizeHandCardPref` 的 `promptEnabled: value.promptEnabled !== false`） | #102 设置守恒：新设置项一换一或默认关。第五批净增 3 项、移除 0 项 | 改默认关，或在本表「已生效」里记下它为什么是例外。改默认值会动 `index.html`，与 #151 同文件，宜等 #151 落地后单独做 |
| 日落条款（新增入口 90 天后自查，用不到的拆除） | **技术上不可执行**：07-18 之后新增的能力（声景、字卡、拍字、字源、灶台模式、印历补帖、月帖、年报、库切换）一个都没埋点，「用不用得到」无从判断 | #102 日落条款 | 前置 = #131 埋点。在 #131 落地前，#102 正文应标注「暂缓执行」，而不是挂一条永远不执行的规则 |
| 冷却期 | 项目史上没出现过一次。PR #118 的第一个 commit 就在合并完 9 个 PR 的当天 | #134 提议作为守恒第六则 | 是否写进 #102 成为第六则，以及冷却期长度 |
