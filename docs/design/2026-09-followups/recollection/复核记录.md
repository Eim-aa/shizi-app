# 拾字「练习回忆页」复核记录

复核日期：2026-09-03  
复核范围：本目录设计原型与六张固定画面，不包含 `项目代码-PR172` 的开发落地。

## 已完成：设计与原型内审

- 六张固定画面尺寸已核对：5 张 390 × 844，1 张 375 × 667。
- 深色空白、深色落笔、深色提示、小屏空白、浅色空白、大字体落笔均已做逐帧视觉检查。
- 页面阅读顺序已核对为：返回 → 进度 → 缺字语境 → 米字格 → 提示 → 撤销 → 重写 → 不会写 → 写好了 → 跳过。
- 原型状态链已验证：空白提交禁用 → 首笔完成后启用 → 撤销至空白再次禁用 → 重写至空白再次禁用 → 仅提示仍禁用 → 提示后落笔才启用。
- 提示完成会朗读“已提示下一笔”，并把焦点交回米字格；动态标签不包含答案。
- 桌面设备切换已核对：375 × 667 画板使用 296 pt 米字格、44 pt 工具行与 52 pt 主按钮。
- 原型运行期间无控制台错误；固定代码快照保持未修改。

## 产品复核结论

设计层面可进入无引导理解测试。朱红已收束为“真实落笔后的提交”单一强焦点；缺字位、米字格、工具与底部操作的层级符合任务书；小屏、浅色和大字体没有发现遮挡或裁切。

## 进入开发前的人测门禁（尚未执行）

本次工作区内没有真实首次用户，不能把内部评审冒充为无引导理解测试。产品应邀请 3–5 位从未使用过拾字的人，只展示“390 × 844 深色、尚未书写”画面，并按 `设计规范.md` 第 7 节逐题记录。

必须同时满足：

1. 参与者能说出要根据拼音和词语空位写字；
2. 能指出缺字位；
3. 能找到提示；
4. 理解提示不会替自己完成书写；
5. 理解空白时不能提交；
6. 能找到撤销与重写。

任一项持续依赖讲解，则本设计不能进入开发，应先回到对应层级迭代并重测。
